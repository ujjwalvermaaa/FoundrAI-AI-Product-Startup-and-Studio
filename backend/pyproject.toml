[tool.poetry]
name = "foundrai-backend"
version = "1.0.0"
description = "FoundrAI – AI Product & Startup Studio – Backend API"
authors = ["FoundrAI Team"]
readme = "README.md"
packages = [{ include = "app" }, { include = "ai", from = ".." }]

[tool.poetry.dependencies]
python = ">=3.11,<3.13"
fastapi = "^0.115.0"
uvicorn = { extras = ["standard"], version = "^0.32.0" }
sqlalchemy = { extras = ["asyncio"], version = "^2.0.0" }
asyncpg = "^0.30.0"
alembic = "^1.14.0"
pydantic = {extras = ["email"], version = "^2.13.4"}
pydantic-settings = "^2.5.0"
python-jose = { extras = ["cryptography"], version = "^3.3.0" }
passlib = { extras = ["bcrypt"], version = "^1.7.4" }
bcrypt = "^4.2.0"
httpx = "^0.28.0"
python-multipart = "^0.0.18"
structlog = "^24.4.0"
tenacity = "^9.0.0"
langchain = "^0.3.0"
langchain-community = "^0.3.0"
langgraph = "^0.2.0"
sentence-transformers = "^3.3.0"
faiss-cpu = "^1.9.0"
numpy = "^1.26.0"
pyyaml = "^6.0.0"
aiofiles = "^24.1.0"
python-dotenv = "^1.0.0"
greenlet = "^3.0.0"

[tool.poetry.group.dev.dependencies]
pytest = "^8.3.0"
pytest-asyncio = "^0.24.0"
pytest-cov = "^6.0.0"
httpx = "^0.28.0"
ruff = "^0.8.0"
mypy = "^1.13.0"
factory-boy = "^3.3.0"
aiosqlite = "^0.22.1"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.12"
strict = false
ignore_missing_imports = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["..", "."]

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
