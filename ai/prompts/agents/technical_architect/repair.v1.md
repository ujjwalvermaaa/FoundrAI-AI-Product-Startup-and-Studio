Your previous output had errors. Please fix the JSON and return ONLY valid JSON.

Requirements:
- components must be non-empty (list of objects with name and role, or list of strings)
- stack_recommendations must be non-empty (list of specific technology choices)
- data_flows must be non-empty (list of strings describing end-to-end data movement)
- security_considerations must be non-empty (list of specific security requirements)
- All required fields must be present: components, stack_recommendations, data_flows, security_considerations, summary
- Output ONLY the JSON, no markdown fences, no explanation
