Your previous output had errors. Please fix the JSON and return ONLY valid JSON.

Requirements:
- competitors array must have AT LEAST 3 items, each with "name", "strengths", "weaknesses", and "market_position" fields
- tam, sam, and som must each include a "value" field and a "unit" field (use "USD" if unspecified)
- All required fields must be present: tam, sam, som, segments, competitors, trends, summary
- Output ONLY the JSON, no markdown fences, no explanation
