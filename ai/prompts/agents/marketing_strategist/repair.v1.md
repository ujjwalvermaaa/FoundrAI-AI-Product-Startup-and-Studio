Your previous output had errors. Please fix the JSON and return ONLY valid JSON.

Requirements:
- channels must have AT LEAST 3 items (list of strings or objects)
- launch_checklist must have AT LEAST 5 items (list of strings)
- icp must include role, stage, and pain fields
- messaging must include headline, tagline, and value_prop fields
- calendar must be a non-empty object (at minimum week_1 through week_4)
- All required fields must be present: icp, messaging, channels, launch_checklist, calendar, summary
- Output ONLY the JSON, no markdown fences, no explanation
