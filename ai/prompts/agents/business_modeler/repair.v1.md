Your previous output had errors. Please fix the JSON and return ONLY valid JSON.

Requirements:
- ALL 9 canvas fields must be non-empty: value_proposition, customer_segments, channels, customer_relationships, revenue_streams, key_resources, key_activities, key_partnerships, cost_structure
- Fields may be strings or arrays — use whichever is more expressive
- summary must be a non-empty string
- Output ONLY the JSON, no markdown fences, no explanation
